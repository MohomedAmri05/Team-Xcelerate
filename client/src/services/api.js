import axios from 'axios';
export const api=axios.create({baseURL:import.meta.env.VITE_API_URL||'http://localhost:5000/api/v1'});api.interceptors.request.use(c=>{const t=sessionStorage.getItem('accessToken');if(t)c.headers.Authorization=`Bearer ${t}`;return c});api.interceptors.response.use(r=>r,e=>{if(e.response?.status===401&&location.pathname.startsWith('/admin'))sessionStorage.removeItem('accessToken');return Promise.reject(e)});
