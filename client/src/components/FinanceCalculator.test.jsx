import {describe,it,expect,vi} from 'vitest';import {render,screen} from '@testing-library/react';import FinanceCalculator from './FinanceCalculator';vi.mock('../hooks/useApi',()=>({useApi:()=>({data:{annualInterestRate:0,minDownPaymentPct:20,maxLoanTermMonths:60}})}));describe('FinanceCalculator',()=>{it('shows an estimate and disclaimer',()=>{render(<FinanceCalculator price={1000000}/>);expect(screen.getByText(/ESTIMATED MONTHLY/i)).toBeInTheDocument();expect(screen.getByText(/not a loan offer/i)).toBeInTheDocument()})});
