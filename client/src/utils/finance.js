export function calculateLoan(price,downPayment,term,annualRate){const principal=price-downPayment;const monthlyRate=annualRate/12/100;const monthlyPayment=monthlyRate===0?principal/term:principal*monthlyRate*(1+monthlyRate)**term/((1+monthlyRate)**term-1);return {principal,downPayment,monthlyPayment,totalRepayment:monthlyPayment*term,estimatedInterest:monthlyPayment*term-principal}}
