import {test,expect} from '@playwright/test';test('mobile navigation opens',async({page})=>{await page.setViewportSize({width:390,height:844});await page.goto('http://localhost:5173');await page.getByLabel('Toggle navigation').click();await expect(page.getByRole('link',{name:'inventory'})).toBeVisible()});test('404 is useful',async({page})=>{await page.goto('http://localhost:5173/not-real');await expect(page.getByText(/road ends here/i)).toBeVisible()});
